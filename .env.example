# Required
OPENAI_API_KEY=sk-your-api-key-here

# AI Configuration
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
SYSTEM_PROMPT=You are a helpful assistant. Keep responses concise for SMS.
MAX_CONTEXT_MESSAGES=20

# Authorization (comma-separated phone numbers, empty = allow all)
ALLOWED_PHONE_NUMBERS=

# Server
HOST=0.0.0.0
PORT=8080

# Optional: Require API key from httpSMS app
SMS_API_KEY=
